import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello Revyl")
                .font(.largeTitle)
            Text("Native iOS Demo")
                .foregroundColor(.gray)
        }
    }
}

#Preview {
    ContentView()
}
